The list in this file does not include daily tasks, but rather changes,
also possibly optional changes, that may take a bit of work and could
modify how the app works for the user.
It can also include bugs that was not practical to fix as soon as they were found.

TODO: allow and manage window resizing
TODO: add Help, incl. About
TODO: add key control: if keyboard focus in numPointsInputField, UP/DOWN -> add/remove a focal point and redraw
TODO: add key control: F5 -> with Random foci, select a new set and redraw
TODO: (suggested by github.com/R1ck77) allow user to drag foci around the screen

